<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TaskManager.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:TaskManager.App.ViewModels"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d">

    <!--
        UI layout is spec §6 "Variant A": a left NavigationView rail; a graph strip pinned
        to the top of the content area (fixed row) with the process list scrolling beneath
        it (star row). The strip shows system-wide CPU + memory, identical across all views.
    -->
    <NavigationView
        x:Name="Nav"
        PaneDisplayMode="Left"
        IsSettingsVisible="False"
        IsBackButtonVisible="Collapsed"
        SelectionChanged="OnNavSelectionChanged">

        <NavigationView.MenuItems>
            <NavigationViewItem Content="Apps" Tag="Apps" IsSelected="True">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xECAA;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Background processes" Tag="Background">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE9F5;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Services" Tag="Services">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE912;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <Grid Padding="20,12,20,16" RowSpacing="14">
            <Grid.Resources>
                <!--
                    One graph card, instantiated twice in the strip (CPU + Memory). Bound to a
                    GraphViewModel so the two cards differ only by the view model they carry.
                -->
                <DataTemplate x:Key="GraphCardTemplate" x:DataType="vm:GraphViewModel">
                    <Border
                        Padding="14,12"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <Grid RowSpacing="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <TextBlock
                                    Text="{x:Bind Title}"
                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock
                                    HorizontalAlignment="Right"
                                    Style="{ThemeResource SubtitleTextBlockStyle}"
                                    Text="{x:Bind ValueText, Mode=OneWay}" />
                            </Grid>
                            <TextBlock
                                Grid.Row="1"
                                Style="{ThemeResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Text="{x:Bind CaptionText, Mode=OneWay}" />
                            <lvc:CartesianChart
                                Grid.Row="2"
                                Height="88"
                                Margin="0,6,0,0"
                                Series="{x:Bind Series}"
                                XAxes="{x:Bind XAxes}"
                                YAxes="{x:Bind YAxes}"
                                TooltipPosition="Hidden"
                                LegendPosition="Hidden"
                                IsHitTestVisible="False" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- graph strip: PINNED -->
                <RowDefinition Height="Auto" />
                <!-- toolbar -->
                <RowDefinition Height="*" />
                <!-- list: SCROLLS beneath the pinned strip -->
            </Grid.RowDefinitions>

            <!-- ============ Graph strip (pinned) ============ -->
            <Grid Grid.Row="0" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ContentControl
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Content="{x:Bind ViewModel.GraphStrip.Cpu}"
                    ContentTemplate="{StaticResource GraphCardTemplate}" />

                <ContentControl
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Content="{x:Bind ViewModel.GraphStrip.Memory}"
                    ContentTemplate="{StaticResource GraphCardTemplate}" />
            </Grid>

            <!-- ============ Toolbar: header + End task ============ -->
            <Grid Grid.Row="1">
                <TextBlock
                    VerticalAlignment="Center"
                    Style="{ThemeResource TitleTextBlockStyle}"
                    Text="{x:Bind ViewModel.HeaderText, Mode=OneWay}" />
                <Button
                    HorizontalAlignment="Right"
                    Command="{x:Bind ViewModel.EndTaskCommand}"
                    Visibility="{x:Bind BoolToVisibility(ViewModel.IsProcessView), Mode=OneWay}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="14" Glyph="&#xE711;" />
                        <TextBlock Text="End task" />
                    </StackPanel>
                </Button>
            </Grid>

            <!-- ============ List surface (scrolls beneath the pinned strip) ============ -->
            <Grid Grid.Row="2">

                <!-- Process list (Apps / Background) -->
                <Grid Visibility="{x:Bind BoolToVisibility(ViewModel.IsProcessView), Mode=OneWay}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- column header -->
                    <Grid Grid.Row="0" Padding="16,4,28,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="90" />
                            <ColumnDefinition Width="110" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Name" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                        <TextBlock Grid.Column="1" Text="CPU" TextAlignment="Right" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                        <TextBlock Grid.Column="2" Text="Memory" TextAlignment="Right" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                    </Grid>

                    <ListView
                        Grid.Row="1"
                        ItemsSource="{x:Bind ViewModel.CurrentProcesses, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.SelectedProcess, Mode=TwoWay}"
                        SelectionMode="Single">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="12,0" />
                                <Setter Property="MinHeight" Value="40" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="vm:ProcessRowViewModel">
                                <Grid RightTapped="OnRowRightTapped">
                                    <Grid.ContextFlyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="End task" Click="OnEndTaskMenuClick">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE711;" />
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                        </MenuFlyout>
                                    </Grid.ContextFlyout>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="110" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel
                                        Grid.Column="0"
                                        Orientation="Horizontal"
                                        Spacing="10"
                                        VerticalAlignment="Center">
                                        <!-- Icon + label (spec §6). A generic app icon (tile +
                                             glyph) shows until/unless the real icon resolves, so
                                             a process whose image path can't be read degrades to
                                             a generic icon rather than erroring (spec §4). -->
                                        <Grid Width="16" Height="16" VerticalAlignment="Center">
                                            <Border
                                                Background="{ThemeResource ControlFillColorSecondaryBrush}"
                                                CornerRadius="3" />
                                            <FontIcon
                                                FontSize="11"
                                                Glyph="&#xECAA;"
                                                Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                            <Image
                                                Width="16"
                                                Height="16"
                                                Source="{x:Bind IconSource, Mode=OneWay}" />
                                        </Grid>
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            Text="{x:Bind Name, Mode=OneWay}"
                                            TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <TextBlock
                                        Grid.Column="1"
                                        VerticalAlignment="Center"
                                        TextAlignment="Right"
                                        FontFamily="{StaticResource TabularNumericFontFamily}"
                                        Text="{x:Bind CpuText, Mode=OneWay}"
                                        Foreground="{Binding CpuHeat, Converter={StaticResource HeatBrush}}" />
                                    <TextBlock
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        TextAlignment="Right"
                                        FontFamily="{StaticResource TabularNumericFontFamily}"
                                        Text="{x:Bind MemoryText, Mode=OneWay}"
                                        Foreground="{Binding MemoryHeat, Converter={StaticResource HeatBrush}}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>

                <!-- Services list (view-only) -->
                <Grid Visibility="{x:Bind BoolToVisibility(ViewModel.IsServicesView), Mode=OneWay}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Padding="16,4,28,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Name" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                        <TextBlock Grid.Column="1" Text="Description" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                        <TextBlock Grid.Column="2" Text="Status" TextAlignment="Right" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                    </Grid>

                    <ListView
                        Grid.Row="1"
                        ItemsSource="{x:Bind ViewModel.Services, Mode=OneWay}"
                        SelectionMode="Single">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="12,0" />
                                <Setter Property="MinHeight" Value="40" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="vm:ServiceRowViewModel">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="3*" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        VerticalAlignment="Center"
                                        Text="{x:Bind DisplayName, Mode=OneWay}"
                                        TextTrimming="CharacterEllipsis" />
                                    <TextBlock
                                        Grid.Column="1"
                                        VerticalAlignment="Center"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind Description, Mode=OneWay}"
                                        TextTrimming="CharacterEllipsis" />
                                    <Border
                                        Grid.Column="2"
                                        HorizontalAlignment="Right"
                                        Padding="10,2"
                                        CornerRadius="10"
                                        Background="{Binding IsRunning, Converter={StaticResource PillBackground}}">
                                        <TextBlock
                                            FontSize="12"
                                            Text="{x:Bind StatusText, Mode=OneWay}"
                                            Foreground="{Binding IsRunning, Converter={StaticResource PillForeground}}" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Grid>
        </Grid>
    </NavigationView>
</Window>
