# CsWin32 binding surface (spec §4 / §9 checklist).
# Each line names a Win32 function, struct, enum, or constant for which the
# C#/Win32 P/Invoke source generator emits a wrapper under Windows.Win32.*.

# --- Process enumeration: no per-process handle needed (spec §4) ---
CreateToolhelp32Snapshot
Process32FirstW
Process32NextW
PROCESSENTRY32W
TH32CS_SNAPPROCESS

# --- Per-process handles: CPU %, memory, terminate ---
OpenProcess
CloseHandle
GetProcessTimes
GetProcessMemoryInfo
PROCESS_MEMORY_COUNTERS_EX2
TerminateProcess
PROCESS_QUERY_LIMITED_INFORMATION
PROCESS_TERMINATE

# --- System-wide CPU + memory (both graphs always work, spec §4) ---
GetSystemTimes
GlobalMemoryStatusEx
GetPerformanceInfo

# --- App vs Background window-based classification (spec §7) ---
EnumWindows
GetWindowThreadProcessId
IsWindowVisible
GetWindow
GetWindowLongPtrW
DwmGetWindowAttribute
DWMWINDOWATTRIBUTE
WINDOW_LONG_PTR_INDEX
WINDOW_EX_STYLE
GET_WINDOW_CMD

# --- Services: enumerate, states, hosting PID, description (spec §4 / §6) ---
OpenSCManagerW
EnumServicesStatusExW
OpenServiceW
QueryServiceConfig2W
CloseServiceHandle
SC_MANAGER_ENUMERATE_SERVICE
SC_ENUM_PROCESS_INFO
SERVICE_QUERY_CONFIG
SERVICE_CONFIG_DESCRIPTION
SERVICE_DESCRIPTIONW
ENUM_SERVICE_TYPE
SERVICE_STATE
SERVICE_STATUS_CURRENT_STATE

# --- Elevation affordance: relaunch elevated via the runas verb (spec §8) ---
ShellExecuteW
